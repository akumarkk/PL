###### operators
set operators
a = {1, 2, 3}\b = {2, 3, 4}
a | b = {1, 2, 3, 4} = union of sets
a - b = {1} = diff of sets
a ^ b = {1, 4} = symmetric diff of sets
a & b = {2, 3} = symmetric diff of sets